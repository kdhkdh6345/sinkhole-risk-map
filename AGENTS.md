# AGENTS.md — 서울시 싱크홀 위험도 지도

이 파일은 저장소 루트에 두며, 에이전트는 세션 시작 시 이 파일을 먼저 읽고 전체 작업에 적용한다.
단계별 작업 명세는 `docs/BUILD_PLAN.md`에 있다. 작업 지시를 받으면 해당 Phase 절을 반드시 먼저 읽는다.

---

## 1. 프로젝트 목적

서울시를 500m×500m 격자로 나누고, 각 격자의 싱크홀 위험도를 1~3단계로 산정해 웹 지도에 표출한다.

산출물은 두 가지다.

1. **실시간 모드** — 공공 API(기상청·GIMS·TOPIS)를 주기적으로 호출해 실제 위험도를 계산한다.
2. **시뮬레이션 모드** — 랜덤/시나리오 값으로 동일 로직을 구동한다. 서버 없이 브라우저에서 단독 동작해야 한다.

두 모드는 **같은 계산 로직을 공유한다.** 데이터를 가져오는 어댑터 계층만 교체된다.
모드별로 계산 코드를 복제하는 구현은 금지한다.

---

## 2. 절대 규칙 (위반 시 작업 중단)

에이전트는 아래 항목을 어기는 코드를 작성하지 않는다. 판단이 서지 않으면 진행하지 말고 사용자에게 질문한다.

1. **API 키를 프론트엔드 코드나 저장소에 절대 커밋하지 않는다.** 키는 GitHub Actions Secrets 또는 `.env`(gitignore 대상)에서만 읽는다. `web/` 아래 어떤 파일에도 키가 등장해서는 안 된다.
2. **`time.time()`을 로직 안에서 직접 호출하지 않는다.** 반드시 주입된 `Clock` 객체를 통해 현재 시각을 얻는다. (사유: 24~72시간 감쇠를 가속 시연해야 함)
3. **배점·임계값을 코드에 숫자로 박지 않는다.** 전부 `config/weights.yaml`에서 읽는다.
4. **격자 인덱스 순서를 변경하지 않는다.** `data/grid.parquet`은 최초 1회 생성 후 불변이다. 재생성이 필요하다고 판단되면 반드시 사용자에게 먼저 확인받는다.
5. **`git push --force`, 브랜치 삭제, 이력 재작성을 하지 않는다.**
6. **외부 API를 테스트 목적으로 반복 호출하지 않는다.** 개발 중에는 `data/fixtures/`에 저장된 응답 샘플을 사용한다.

---

## 3. 기술 스택 (고정)

변경하려면 사용자 승인이 필요하다.

| 영역 | 선택 | 비고 |
|---|---|---|
| 계산 | Python 3.11+ | numpy, pandas, geopandas, pyyaml, requests |
| 검정 | pytest | |
| 프론트 | 순수 HTML/CSS/JavaScript | 빌드 도구 없음. 번들러·프레임워크 도입 금지 |
| 지도 | Leaflet 1.9 (CDN) | |
| 배포 | GitHub Pages (`/docs` 또는 `gh-pages`) | |
| 스케줄 | GitHub Actions cron | |

프론트에 React·Vue·Vite를 도입하지 않는다. GitHub Pages에 정적 파일 그대로 올라가야 한다.

---

## 4. 저장소 구조

```
.
├── AGENTS.md
├── docs/BUILD_PLAN.md          # 단계별 작업 명세
├── config/
│   ├── grid.yaml               # 격자 정의, 감쇠 파라미터
│   └── weights.yaml            # 배점, 임계값, 단계판정 규칙
├── src/sinkhole/
│   ├── grid/                   # 격자 생성, 좌표→격자 매핑
│   ├── static_layers/          # 정적 기저점수 B 계산
│   ├── sources/                # 데이터 소스 어댑터
│   ├── fusion/                 # 감쇠, 융합, 이상탐지, 점수화
│   ├── core/                   # 상태 관리, Clock, 스냅샷 저장
│   └── cli.py                  # 진입점
├── scripts/
│   ├── build_grid.py
│   ├── build_baseline.py
│   └── run_snapshot.py         # Actions가 호출하는 1회 실행 스크립트
├── web/                        # 정적 사이트 (GitHub Pages 루트)
│   ├── index.html
│   ├── js/{map.js, sim.js, render.js}
│   └── data/{grid.json, snapshot.json}
├── data/
│   ├── grid.parquet            # 불변
│   ├── baseline.npy
│   └── fixtures/               # API 응답 샘플
├── tests/
└── .github/workflows/snapshot.yml
```

---

## 5. 데이터 계약

이 스키마는 Python 쪽과 웹 쪽의 계약이다. **양쪽 중 한쪽만 바꾸는 변경은 금지한다.**

### 5.1 `web/data/grid.json` — 격자 정의 (불변, 최초 1회 생성)

```json
{
  "grid_size_m": 500,
  "count": 2418,
  "cells": [
    { "id": 0, "lat": 37.4135, "lon": 126.7645, "gu": "강서구" }
  ]
}
```

### 5.2 `web/data/snapshot.json` — 위험도 스냅샷 (5분마다 갱신)

좌표는 넣지 않는다. `id`로 `grid.json`과 조인한다.

```json
{
  "generated_at": "2026-08-25T14:35:00+09:00",
  "mode": "real",
  "source_status": { "rain": "ok", "groundwater": "ok", "traffic": "stale" },
  "cells": [
    { "id": 1042, "stage": 2, "score": 48.3,
      "b": 34.1, "r": 15.0, "g": 0.0, "t": 2.0, "unc": 5.2 }
  ]
}
```

| 필드 | 의미 |
|---|---|
| `stage` | 1(초록) / 2(노랑) / 3(빨강) |
| `score` | 총점 0~100 |
| `b` | 정적 기저점수 0~60 |
| `r` / `g` / `t` | 강수·지하수위·교통 실시간 가점 (감쇠 적용 후) |
| `unc` | 융합 추정 불확실성(표준편차). Phase 6 이전에는 `null` |

`b/r/g/t`를 분리해 싣는 이유는 지도에서 격자를 클릭했을 때 "왜 이 단계인지"를 항목별로 보여주기 위함이다. 합산값만 저장하는 구현은 거부한다.

### 5.3 갱신 실패 시 동작

계산 워커가 실패해도 기존 `snapshot.json`을 덮어쓰지 않는다. 프론트는 `generated_at`을 화면에 표시하고, 20분 이상 지났으면 "갱신 지연" 배지를 띄운다. **빈 화면이나 에러 화면을 보여주지 않는다.**

---

## 6. 점수 설계 (config/weights.yaml에 반영)

### 정적 기저점수 B (0~60)

| 요소 | 배점 | 정규화 |
|---|---|---|
| 하수관 노후도·누수 | 0~20 | 자치구별 노후 비율 min-max |
| 시추공 지층 / N값 | 0~15 | N값 역수 + 매립층 여부 |
| 과거 침하·공동 이력 | 0~15 | 반경 내 건수 × 최근성 감쇠 |
| 액상화 위험등급 | 0~10 | 상/중/하 → 1.0 / 0.6 / 0.2 |

### 실시간 가중치 (0~40)

**강수 R (0~25)** — 기상청 특보 기준을 그대로 매핑한다.

| 구간 | 조건 | 점수 |
|---|---|---|
| 사전 주의 | 1h ≥ 20mm 또는 3h ≥ 40mm | 10 |
| 호우주의보 | 3h ≥ 60mm 또는 12h ≥ 110mm | 15 |
| 호우경보 | 3h ≥ 90mm 또는 12h ≥ 180mm | 20 |
| 극한호우 | 1h ≥ 50mm이면서 3h ≥ 90mm, 또는 1h ≥ 72mm | 25 |

**지하수위 G (0~10)** — 관측정별 최근 30일 평균 대비. 1σ 이상 급락 +5, 2σ 이상 +10.
단, **R ≥ 15일 때만 인정**한다(강수 없는 단독 변동 배제). 2회 연속 관측에서 유지될 때만 인정한다(센서 오류 필터).

**교통 T (0~5)** — 초기 구현은 정적 프록시(도로 등급, 버스노선 수)를 사용한다. TOPIS 실시간 연동은 Phase 7 이후 선택 사항이다.

### 단계 판정

```
1단계: B >= 30
2단계: 1단계 AND R >= 15
3단계: 2단계 AND G >= 10
```

`B >= 30`은 잠정값이다. 전체 격자 점수 분포를 뽑은 뒤 percentile로 튜닝하며, 그 근거를 `docs/tuning.md`에 기록한다.

### 감쇠

- 0~24시간: plateau(감쇠 없음, factor = 1.0)
- 24~72시간: 지수감쇠. 72시간 시점 잔존비율 0.05
- 72시간 초과: 0

강수·지하수위·교통 세 채널이 **동일한 감쇠 함수 하나를 공유한다.** 채널별로 별도 함수를 만들지 않는다.

---

## 7. 코딩 규칙

- 격자 상태는 격자당 객체가 아니라 **길이 N의 numpy 배열 세트**로 관리한다. 반복문 대신 벡터 연산을 쓴다.
- 결측은 `np.nan`으로 두고, 점수 계산 시 해당 채널만 제외한다. 0으로 채워 정상값처럼 취급하지 않는다.
- 각 채널은 **원본 단위에서 이상도를 먼저 계산한 뒤 격자로 투영한다.** 순서를 뒤집지 않는다.
  - 강수: 격자 래스터 → 그대로
  - 지하수위: 관측정 단위로 σ 계산 → 격자 보간
  - 교통: 도로 링크 단위로 저하율 계산 → 격자 평균
- 모든 소스 어댑터는 `sources/base.py`의 추상 클래스를 상속하고 동일한 시그니처를 가진다.
- 함수·클래스에 한국어 docstring을 단다. 수식이 들어가는 함수에는 근거(기준 출처)를 주석으로 남긴다.
- 파일당 300줄을 넘기지 않는다. 넘으면 모듈을 분리한다.
- 커밋 메시지는 한국어로, `[Phase N] 요약` 형식.

---

## 8. 작업 진행 방식

1. Phase 하나를 지시받으면 `docs/BUILD_PLAN.md`의 해당 절을 읽고 **구현 계획을 먼저 제시한 뒤 승인을 기다린다.**
2. 구현 후 해당 Phase의 **수용 기준을 하나씩 실제로 실행해서 검증한다.** 코드를 읽고 "맞을 것"이라고 판단하지 않는다.
3. 검증 결과를 출력값과 함께 보고한다.
4. Phase를 건너뛰거나 여러 Phase를 한 번에 진행하지 않는다.

수용 기준을 통과하지 못하면 다음 Phase로 넘어가지 않는다.
